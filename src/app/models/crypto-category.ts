export class CryptoCategory {
    name: string;
    abbreviation: string;
    iconPath: string;

    constructor(name: string, abbreviation: string, iconPath: string) {
        this.name = name;
        this.abbreviation = abbreviation;
        this.iconPath = iconPath;
    }
}
